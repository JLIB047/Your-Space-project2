<div class="comment_text">
    <div class="card-header">
       <h4><p class="button" href="/post/{{id}}">{{title}}</p></h4>
        <p>Posted by {{user.username}} on {{format_date created_at}}</p>
    </div>
        <a href="{{post_url}}" target="_blank">Check it Out!</a>
</div>
